export interface App {
  id: string;
  shortId: string;
  name: string;
  altName: string;
  bundleIdentifier: string;
  iconUrl: string;
  version: string;
  buildNumber: string;
  bundleUrl: string;
}
